# setup_env
Bunch of scripts and config files to setup a new environment (mac and VM)


## Setup env
Run `python3 -c "$(curl -fsSL https://raw.githubusercontent.com/nicolascochin/setup_env/master/init_env.py)"`
